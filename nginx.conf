server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        auth_basic "Japan HazLoc Quoting";
        auth_basic_user_file /etc/nginx/.htpasswd;
        try_files $uri $uri/ /index.html;
    }
}
